[package]
name = "vendetta_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
vendeta_core = { path = "../../core" }

[profile.release]
opt-level = "z"
strip = true
lto = true
